(function(requirejs) {
    'use strict';

    // Load the app. This is kept minimal so it doesn't need much updating.
    require(
            [ 'angular', 'jquery', 'bootstrap', './ryvr'  ],
            function(angular) {
            });
})(requirejs);